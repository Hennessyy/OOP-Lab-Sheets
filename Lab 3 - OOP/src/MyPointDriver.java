import javax.swing.*;

/*public class MyPointDriver {
    public static void main(String[] args) {

        MyPoint point = new MyPoint();

        JOptionPane.showMessageDialog(null,point.toString());

        point.moveVertically(Double.parseDouble(JOptionPane.showInputDialog("Enter vertical movement value")));
        point.moveHorizontally(Double.parseDouble(JOptionPane.showInputDialog("Enter horizontal movement value")));

        JOptionPane.showMessageDialog(null,point.distanceFromOrigin());

        double translateX, translateY=0.0f;

        translateX=Double.parseDouble(JOptionPane.showInputDialog("Enter vertical movement value"));
        translateY=Double.parseDouble(JOptionPane.showInputDialog("Enter horizontal movement value"));

        point.translate(translateX,translateY);

        JOptionPane.showMessageDialog(null,point.toString());


    }
}
*/